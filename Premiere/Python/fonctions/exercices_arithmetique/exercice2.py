"""Écrire une fonction est_entier(x:float)->bool qui reçoit en entrée un nombre flottant x et qui renvoie en sortie si OUI (True) ou NON (False) le nombre x est entier."""

def est_entier(x:float)->bool:
    return x >= 0